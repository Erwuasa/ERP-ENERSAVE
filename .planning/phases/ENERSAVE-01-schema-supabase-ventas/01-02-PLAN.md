---
phase: ENERSAVE-01-schema-supabase-ventas
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - supabase/migrations/20260617000004_create_ventas_rls.sql
  - supabase/tests/ventas_rls.test.sql
autonomous: true
requirements:
  - DATA-02
must_haves:
  truths:
    - "RLS enabled on prospectos, actividades_ventas, tareas_ventas, and erp_comerciales"
    - "Comercial role cannot SELECT prospectos where comercial_id is outside accessible set"
    - "Jefe_comercial can SELECT prospectos for direct reports via manager_id hierarchy"
    - "Superadmin can SELECT all prospectos"
    - "actividades_ventas has no UPDATE/DELETE policies for comercial/jefe roles (immutable timeline)"
  artifacts:
    - path: supabase/migrations/20260617000004_create_ventas_rls.sql
      provides: "private schema helpers and RLS policies"
      contains: "create schema if not exists private"
    - path: supabase/tests/ventas_rls.test.sql
      provides: "SQL policy verification script"
      contains: "accessible_comercial_ids"
  key_links:
    - from: supabase/migrations/20260617000004_create_ventas_rls.sql
      to: supabase/migrations/20260617000001_create_erp_comerciales.sql
      via: "private.accessible_comercial_ids reads erp_comerciales"
      pattern: "erp_comerciales"
    - from: supabase/migrations/20260617000004_create_ventas_rls.sql
      to: public.prospectos
      via: "comercial_id in (select private.accessible_comercial_ids())"
      pattern: "accessible_comercial_ids"
---

<objective>
Implement Row Level Security for ventas tables using erp_comerciales hierarchy and private SECURITY DEFINER helpers, with SQL tests proving cross-comercial isolation.

Purpose: DATA-02 — authorization enforced in Postgres, not only client filters.
Output: RLS migration + supabase/tests/ventas_rls.test.sql
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/ENERSAVE-01-schema-supabase-ventas/01-RESEARCH.md
@supabase/migrations/20260617000001_create_erp_comerciales.sql
@supabase/migrations/20260617000002_create_ventas_core.sql
@src/lib/supabase/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create private schema helpers and enable RLS</name>
  <read_first>
    - .planning/phases/ENERSAVE-01-schema-supabase-ventas/01-RESEARCH.md (RLS Strategy section)
    - src/lib/supabase/client.ts (anon key, no JWT today)
    - Locked decision: text comercial_id usr-* convention; bridge via erp_comerciales
  </read_first>
  <files>supabase/migrations/20260617000004_create_ventas_rls.sql</files>
  <action>
    Create supabase/migrations/20260617000004_create_ventas_rls.sql starting with:

    1. CREATE SCHEMA IF NOT EXISTS private — document in comment: do NOT expose private in Supabase API settings

    2. Helper functions (all SECURITY DEFINER, SET search_path = public):
       - private.current_comercial_id() — coalesce JWT app_metadata.comercial_id, lookup erp_comerciales by auth_user_id
       - private.current_role() — coalesce JWT app_metadata.role, lookup erp_comerciales.role
       - private.accessible_comercial_ids() — RETURNS SETOF text:
         * superadmin → all erp_comerciales.id
         * jefe_comercial → self + ids where manager_id = current_comercial_id()
         * comercial → self only

    3. ALTER TABLE ... ENABLE ROW LEVEL SECURITY on erp_comerciales, prospectos, actividades_ventas, tareas_ventas

    4. GRANT USAGE ON SCHEMA private TO authenticated (not anon)
    5. GRANT SELECT on erp_comerciales to authenticated for helper lookups

    Wrap helper calls in policies as (select private.current_comercial_id()) per Supabase performance guidance.

    Do NOT use USING (true) on ventas tables. Do NOT copy UUID-only supabase-setup.sql policies verbatim.
  </action>
  <acceptance_criteria>
    - Migration creates private schema and three helper functions
    - RLS enabled on all four ventas-related tables
    - No policy uses USING (true) on prospectos
    - Functions use SECURITY DEFINER with explicit search_path
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^--' supabase/migrations/20260617000004_create_ventas_rls.sql | grep -c 'accessible_comercial_ids' | awk '{exit ($1>=1)?0:1}'</automated>
    <automated>grep -v '^--' supabase/migrations/20260617000004_create_ventas_rls.sql | grep -c 'ENABLE ROW LEVEL SECURITY' | awk '{exit ($1>=4)?0:1}'</automated>
  </verify>
  <done>Private helpers and RLS enablement committed; schema ready for policies.</done>
</task>

<task type="auto">
  <name>Task 2: Add per-operation RLS policies on ventas tables</name>
  <read_first>
    - .planning/phases/ENERSAVE-01-schema-supabase-ventas/01-RESEARCH.md (policies table, child table inheritance, immutability)
    - Pitfall 4: SELECT policy required alongside UPDATE with same USING clause
  </read_first>
  <files>supabase/migrations/20260617000004_create_ventas_rls.sql</files>
  <action>
    Append to same migration file (20260617000004) policies per RESEARCH.md:

    prospectos:
    - SELECT authenticated: comercial_id IN (SELECT private.accessible_comercial_ids())
    - INSERT authenticated: comercial_id = (SELECT private.current_comercial_id()) OR role in (jefe_comercial, superadmin) with WITH CHECK matching scope
    - UPDATE authenticated: same USING and WITH CHECK; superadmin may reassign comercial_id; others cannot change comercial_id to out-of-scope id
    - DELETE authenticated: superadmin only (or omit DELETE for non-superadmin)

    actividades_ventas:
    - SELECT via prospecto_id IN (SELECT id FROM prospectos WHERE comercial_id IN accessible set)
    - INSERT WITH CHECK prospecto in scope and comercial_id matches actor
    - NO UPDATE/DELETE policies for comercial/jefe (immutable timeline)

    tareas_ventas:
    - SELECT/INSERT/UPDATE: comercial_id IN accessible set (UPDATE WITH CHECK preserves scope)
    - DELETE: superadmin only or soft via estado

    erp_comerciales:
    - SELECT authenticated: id IN accessible set OR role = superadmin (managers need to see team)
    - INSERT/UPDATE/DELETE: superadmin only

    Pair SELECT + UPDATE policies on prospectos and tareas with identical USING expressions.

    Document in migration header: anon key without JWT returns zero rows — expected until auth milestone.
  </action>
  <acceptance_criteria>
    - prospectos has separate policies for SELECT, INSERT, UPDATE
    - actividades_ventas has SELECT and INSERT only (no UPDATE policy for authenticated comercial path)
    - Child tables scope via prospecto join or denormalized comercial_id
    - Policy names follow consistent naming (e.g., prospectos_select_authenticated)
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^--' supabase/migrations/20260617000004_create_ventas_rls.sql | grep -c 'create policy' | awk '{exit ($1>=6)?0:1}'</automated>
    <automated>grep -v '^--' supabase/migrations/20260617000004_create_ventas_rls.sql | grep -ic 'actividades_ventas.*update' | awk '{exit ($1==0)?0:1}'</automated>
  </verify>
  <done>Full policy set committed; immutability enforced on actividades_ventas.</done>
</task>

<task type="auto">
  <name>Task 3: Add SQL RLS verification script</name>
  <read_first>
    - .planning/phases/ENERSAVE-01-schema-supabase-ventas/01-RESEARCH.md (Interim verification, Validation Architecture)
    - erp_comerciales seed usr-3 (comercial) and usr-4 (comercial under usr-2)
  </read_first>
  <files>supabase/tests/ventas_rls.test.sql</files>
  <action>
    Create supabase/tests/ventas_rls.test.sql — runnable in SQL Editor after migrations + RLS apply:

    1. Seed two test prospectos: one comercial_id usr-3, one usr-4 (use gen_random_uuid or fixed UUIDs documented in file)

    2. Test blocks using set local role authenticated and request.jwt.claims or set_config to simulate:
       - JWT app_metadata: { "comercial_id": "usr-3", "role": "comercial" }
       - SELECT prospectos returns only usr-3 row (not usr-4)

    3. Second block: role jefe_comercial usr-2 sees usr-3 and usr-4 (direct reports) but not unrelated usr-5 if suspended/out of tree

    4. Third block: superadmin usr-1 sees all seeded prospectos

    5. Assert actividades_ventas UPDATE denied (0 policies or permission error)

    Include commented instructions for Dashboard "Test policy" with sample JWT payload.

    Apply 20260617000004 migration via same method as 01-01 (db push or SQL Editor) before running tests.
  </action>
  <acceptance_criteria>
    - Test file documents JWT claim shape for app_metadata.comercial_id and role
    - Test file includes explicit assertion that usr-3 cannot read usr-4 prospecto
    - Test file includes jefe and superadmin positive cases
    - File header states prerequisite: migrations 01-01 through 01-02 applied
  </acceptance_criteria>
  <verify>
    <automated>test -f supabase/tests/ventas_rls.test.sql && grep -v '^--' supabase/tests/ventas_rls.test.sql | grep -c 'usr-3' | awk '{exit ($1>=1)?0:1}'</automated>
    <automated>grep -v '^--' supabase/tests/ventas_rls.test.sql | grep -c 'accessible_comercial_ids\|comercial_id' | awk '{exit ($1>=2)?0:1}'</automated>
  </verify>
  <done>SQL RLS test script committed; DATA-02 verifiable without live React auth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser anon client → PostgREST | Untrusted; must not bypass RLS |
| JWT app_metadata → private helpers | Trusted claim source when auth ships |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Information disclosure | prospectos SELECT | mitigate | RLS deny-by-default; comercial_id IN accessible_comercial_ids() |
| T-01-06 | Tampering | INSERT forged comercial_id | mitigate | WITH CHECK on INSERT/UPDATE; superadmin-only reassignment |
| T-01-07 | Repudiation | actividades_ventas UPDATE | mitigate | No UPDATE/DELETE policies for standard roles |
| T-01-08 | Elevation | private.* SECURITY DEFINER | mitigate | private schema not API-exposed; fixed search_path |
| T-01-09 | Spoofing | Missing JWT claims | accept | Documented: anon returns []; SQL tests use mock JWT until auth phase |
</threat_model>

<verification>
- Migration 20260617000004 applies after 01-01 migrations without error
- ventas_rls.test.sql documents cross-comercial denial case
- private schema not listed in Supabase exposed schemas (manual Dashboard check noted in SUMMARY)
</verification>

<success_criteria>
- DATA-02 satisfied: hierarchy RLS policies committed and SQL-testable
- Comercial isolation provable via test script
</success_criteria>

<artifacts_this_phase_produces>
| Artifact | Plan | Description |
|----------|------|-------------|
| supabase/migrations/20260617000004_create_ventas_rls.sql | 01-02 | RLS helpers and policies |
| supabase/tests/ventas_rls.test.sql | 01-02 | Policy verification SQL |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/ENERSAVE-01-schema-supabase-ventas/01-02-SUMMARY.md` when done
</output>
