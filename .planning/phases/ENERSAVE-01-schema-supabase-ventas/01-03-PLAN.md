---
phase: ENERSAVE-01-schema-supabase-ventas
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/lib/ventas/types.ts
  - src/lib/supabase/ventas.ts
autonomous: true
requirements:
  - DATA-03
  - DATA-04
must_haves:
  truths:
    - "src/lib/ventas/types.ts exports Prospecto, ActividadVenta, TareaVenta and all enum unions without any"
    - "src/lib/supabase/ventas.ts exports CRUD functions returning VentasResult discriminated unions"
    - "Row mappers convert snake_case DB columns to camelCase domain fields"
    - "updateProspectoFase does not insert cambio_fase activity (trigger owns that)"
    - "npm run lint passes with new files"
  artifacts:
    - path: src/lib/ventas/types.ts
      provides: "Domain types and enums for ventas module"
      exports:
        - ProspectoFase
        - Prospecto
        - ActividadVenta
        - TareaVenta
    - path: src/lib/supabase/ventas.ts
      provides: "Supabase data access layer with mappers"
      exports:
        - listProspectos
        - createProspecto
        - updateProspectoFase
        - listActividades
        - listTareas
  key_links:
    - from: src/lib/supabase/ventas.ts
      to: src/lib/ventas/types.ts
      via: "mapProspectoRow returns Prospecto"
      pattern: "mapProspectoRow"
    - from: src/lib/supabase/ventas.ts
      to: src/lib/supabase/client.ts
      via: "getSupabaseClient()"
      pattern: "getSupabaseClient"
    - from: src/lib/supabase/ventas.ts
      to: public.prospectos
      via: ".from('prospectos')"
      pattern: "from\\(\"prospectos\"\\)"
---

<objective>
Deliver TypeScript domain types and Supabase client functions for ventas, mirroring contracts.ts patterns with snake_case row mappers and discriminated-union results.

Purpose: DATA-03 and DATA-04 — hooks in Phase 3 consume typed, mapper-backed API without any.
Output: src/lib/ventas/types.ts and src/lib/supabase/ventas.ts
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/ENERSAVE-01-schema-supabase-ventas/01-RESEARCH.md
@src/lib/supabase/contracts.ts
@src/lib/supabase/client.ts
@supabase/migrations/20260617000002_create_ventas_core.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ventas domain types (DATA-03)</name>
  <read_first>
    - .planning/phases/ENERSAVE-01-schema-supabase-ventas/01-RESEARCH.md (TypeScript Mapping section)
    - src/lib/incidencias.ts or src/types/contract.ts (enum/interface conventions)
    - Locked decision: 10 pipeline fases and TASK-03 tarea tipos (for TareaTipo union)
  </read_first>
  <files>src/lib/ventas/types.ts</files>
  <action>
    Create src/lib/ventas/types.ts exporting:

    String union types:
    - ProspectoFase (10 values: prospecto_nuevo … descartado)
    - ActividadTipo (nota, llamada, email, reunion, cambio_fase, contrato_creado)
    - TareaTipo (7 TASK-03 values)
    - TareaEstado (pendiente, completada, descartada)
    - TareaPrioridad (alta, media, baja)

    Interfaces (camelCase, optional fields use ?):
    - Prospecto — all fields from RESEARCH domain example including energy, propuesta, address, metadata as Record&lt;string, unknown&gt;, dates as ISO strings
    - ActividadVenta — immutable timeline shape
    - TareaVenta — queue shape with origenFase optional

    Input helper types (for ventas.ts builders):
    - CreateProspectoInput (required nombre, comercialId, comercialName; optional contact/energy fields)
    - CreateActividadInput, CreateTareaInput, UpdateProspectoPatch, UpdateTareaPatch

    Rules:
    - No any anywhere (use Record&lt;string, unknown&gt; for metadata)
    - No runtime code except type exports
    - Match column set from 20260617000002 migration (read migration after 01-01 completes; align field names)
  </action>
  <acceptance_criteria>
    - File exports ProspectoFase, Prospecto, ActividadVenta, TareaVenta
    - grep for ": any" or "as any" in types.ts returns zero matches
    - ProspectoFase union contains exactly 10 fase literals
    - TareaTipo union contains all 7 TASK-03 tipo values
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
    <automated>grep -v '^//' src/lib/ventas/types.ts 2>/dev/null | grep -c ': any' | awk '{exit ($1==0)?0:1}'</automated>
  </verify>
  <done>Domain types compile cleanly and cover full ventas schema surface.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement row types, mappers, and result unions (DATA-04 part 1)</name>
  <read_first>
    - src/lib/supabase/contracts.ts (TeamContractInsert, buildTeamContractRow, SaveTeamContractResult pattern)
    - src/lib/supabase/client.ts (isSupabaseConfigured, getSupabaseClient)
    - src/lib/ventas/types.ts (from Task 1)
  </read_first>
  <files>src/lib/supabase/ventas.ts</files>
  <action>
    Create src/lib/supabase/ventas.ts with:

    1. Row interfaces (snake_case matching DB): ProspectoRow, ActividadVentaRow, TareaVentaRow plus Insert/Update variants

    2. VentasResult&lt;T&gt; discriminated union:
       ok: true | ok: false with reason: not_configured | table_missing | rls_denied | error

    3. Mappers:
       - mapProspectoRow, mapActividadRow, mapTareaRow — optional fields use ?? undefined
       - buildProspectoInsert, buildActividadInsert, buildTareaInsert — domain → row

    4. Shared error helper mapSupabaseError(error) detecting 42P01 → table_missing, 42501 / "row-level security" → rls_denied

    5. Guard at top of each exported async function: isSupabaseConfigured() → not_configured result

    Mirror naming and structure from contracts.ts; do not throw for expected failures.
  </action>
  <acceptance_criteria>
    - ventas.ts exports mapProspectoRow and VentasResult type
    - Error mapping includes rls_denied reason string
    - Row builders produce snake_case keys (comercial_id not comercialId)
    - No any in ventas.ts
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
    <automated>grep -v '^//' src/lib/supabase/ventas.ts | grep -c 'rls_denied' | awk '{exit ($1>=1)?0:1}'</automated>
  </verify>
  <done>Mapper layer and result types implemented following contracts.ts conventions.</done>
</task>

<task type="auto">
  <name>Task 3: Implement CRUD functions and smoke verification (DATA-04 part 2)</name>
  <read_first>
    - .planning/phases/ENERSAVE-01-schema-supabase-ventas/01-RESEARCH.md (Client API Patterns function table)
    - Locked decision: trigger owns cambio_fase — updateProspectoFase must only PATCH fase (+ motivo_descarte when descartado)
  </read_first>
  <files>src/lib/supabase/ventas.ts</files>
  <action>
    Add exported async functions to ventas.ts:

    Prospectos:
    - listProspectos({ comercialId?, fase? }) — .from('prospectos').select('*').eq('comercial_id', …) when filter provided; order updated_at desc
    - getProspecto(id)
    - createProspecto(input) — default fase prospecto_nuevo
    - updateProspecto(id, patch)
    - updateProspectoFase(id, fase, motivoDescarte?) — UPDATE fase/motivo_descarte only; do NOT insert actividades_ventas for cambio_fase

    Actividades:
    - listActividades(prospectoId) — order created_at desc
    - createActividad(input) — manual types only (nota, llamada, etc.); reject tipo cambio_fase in builder or document as trigger-only

    Tareas:
    - listTareas(comercialId, { estado?, fechaDesde? })
    - createTarea(input)
    - updateTarea(id, patch) — complete/dismiss via estado + completada_at when completada

    All return VentasResult&lt;T&gt;. Use .select().single() on writes where id needed.

    Add brief module-level comment listing functions and noting RLS may return empty until JWT auth (expected).

    Verification: if Supabase configured, optional dev-only smoke in SUMMARY (manual listProspectos call); automated gate is lint + export grep.
  </action>
  <acceptance_criteria>
    - All 10 functions from RESEARCH Client API table exported from ventas.ts
    - updateProspectoFase does not call createActividad or insert cambio_fase
    - listProspectos accepts comercialId filter for performance per RLS guidance
    - npm run lint exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
    <automated>grep -v '^//' src/lib/supabase/ventas.ts | grep -c 'export async function' | awk '{exit ($1>=8)?0:1}'</automated>
    <automated>grep -v '^//' src/lib/supabase/ventas.ts | grep -c 'cambio_fase' | awk '{exit ($1>=1)?0:1}'</automated>
  </verify>
  <done>Full ventas Supabase client ready for Phase 3 hooks; lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ventas.ts → PostgREST | Parameterized queries only; no raw SQL strings from user input |
| Domain mappers | Untrusted DB rows parsed into typed domain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Tampering | createProspecto comercialId | mitigate | RLS WITH CHECK enforces scope; client still sends comercial_id from active user in Phase 3 |
| T-01-11 | Information disclosure | listProspectos | mitigate | RLS primary control; client .eq('comercial_id') for perf per RESEARCH |
| T-01-12 | Repudiation | createActividad | transfer | DB immutability policies from 01-02; client does not expose delete |
| T-01-13 | Tampering | metadata jsonb | mitigate | Typed Record&lt;string, unknown&gt;; no eval; PostgREST parameterized |
</threat_model>

<verification>
- npm run lint passes
- types.ts and ventas.ts contain zero `any`
- All DATA-03/DATA-04 exports present per must_haves
</verification>

<success_criteria>
- DATA-03: types.ts reflects schema without any
- DATA-04: ventas.ts provides mapper-backed CRUD with VentasResult errors including rls_denied
</success_criteria>

<artifacts_this_phase_produces>
| Artifact | Plan | Description |
|----------|------|-------------|
| src/lib/ventas/types.ts | 01-03 | Domain types and enums |
| src/lib/supabase/ventas.ts | 01-03 | Mappers and CRUD functions |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/ENERSAVE-01-schema-supabase-ventas/01-03-SUMMARY.md` when done
</output>
