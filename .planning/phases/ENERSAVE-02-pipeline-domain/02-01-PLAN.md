---
phase: ENERSAVE-02-pipeline-domain
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - src/lib/ventas/pipeline.ts
  - src/lib/ventas/pipeline.test.ts
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
must_haves:
  truths:
    - "All 10 ProspectoFase values have label, kanbanOrder, and isTerminal in PIPELINE_FASE_CONFIG"
    - "PIPELINE_KANBAN_COLUMNS exports 10 columns sorted by kanbanOrder for Phase 4 UI"
    - "Terminal fases cliente_activo and descartado have empty outgoing transitions"
    - "canTransition rejects same-fase and invalid adjacency per TRANSITIONS graph"
    - "validateTransition returns Spanish error messages before any Supabase call"
    - "Descarte to descartado requires non-empty motivoDescarte in validateTransition"
  artifacts:
    - path: src/lib/ventas/pipeline.ts
      provides: "Pipeline fase config, transition graph, validation helpers"
      exports:
        - PIPELINE_FASE_CONFIG
        - PIPELINE_KANBAN_COLUMNS
        - TRANSITIONS
        - canTransition
        - getNextFases
        - validateTransition
        - isTerminalFase
    - path: src/lib/ventas/pipeline.test.ts
      provides: "Unit tests for config and transitions"
      contains: "describe"
  key_links:
    - from: src/lib/ventas/pipeline.ts
      to: src/lib/ventas/types.ts
      via: "import type ProspectoFase"
      pattern: "from \"./types\""
    - from: src/lib/ventas/pipeline.test.ts
      to: src/lib/ventas/pipeline.ts
      via: "vitest imports"
      pattern: "from \"./pipeline\""
---

<objective>
Deliver the core pipeline domain module: fase metadata, kanban column order, directed transition graph, and validateTransition gate for Phase 3 hooks.

Purpose: PIPE-01 and PIPE-02 — centralize pipeline rules in a pure, UI-free module mirroring contract-estado.ts; block invalid fase changes before updateProspectoFase.
Output: src/lib/ventas/pipeline.ts, pipeline.test.ts, vitest devDependency and config.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/ENERSAVE-02-pipeline-domain/02-RESEARCH.md
@.planning/phases/ENERSAVE-02-pipeline-domain/PATTERNS.md
@.planning/phases/ENERSAVE-02-pipeline-domain/02-VALIDATION.md
@.planning/phases/ENERSAVE-01-schema-supabase-ventas/01-03-SUMMARY.md
@src/lib/contract-estado.ts
@src/lib/ventas/types.ts
@src/lib/supabase/ventas.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — install vitest and scaffold pipeline module</name>
  <read_first>
    - .planning/phases/ENERSAVE-02-pipeline-domain/02-RESEARCH.md (Standard Stack, Package Legitimacy Audit, Vitest config)
    - .planning/phases/ENERSAVE-02-pipeline-domain/02-VALIDATION.md (Wave 0 Requirements)
    - vite.config.ts (current defineConfig from vite — extend, do not replace plugins)
    - package.json (no vitest yet)
  </read_first>
  <files>package.json, vite.config.ts, src/lib/ventas/pipeline.ts, src/lib/ventas/pipeline.test.ts</files>
  <action>
    Install vitest as devDependency per RESEARCH Package Legitimacy Audit ([ASSUMED] OK — no new runtime deps).

    package.json: add script "test": "vitest run" (keep existing scripts unchanged).

    vite.config.ts: change import to defineConfig from "vitest/config" (re-export still works with react + tailwind plugins); add test block: environment "node", include ["src/**/*.test.ts"]. Preserve existing plugins, alias, server.hmr/watch settings exactly.

    Create src/lib/ventas/pipeline.ts skeleton:
    - import type { ProspectoFase } from "./types"
    - Export placeholder const arrays and function stubs so pipeline.test.ts can import (return safe defaults until Task 2–3 fill logic)
    - Named exports only; no Supabase or React imports
    - Do NOT duplicate ProspectoFase union literals in a second type — import from types.ts only

    Create src/lib/ventas/pipeline.test.ts with empty describe block or one skipped placeholder so vitest run exits 0 until Task 3 adds real tests.

    Locked decisions: ventas module lives in src/lib/ventas/; no UI files in Phase 2.
  </action>
  <acceptance_criteria>
    - package.json contains script "test": "vitest run"
    - vite.config.ts contains test block with environment "node"
    - npx vitest run --version exits 0
    - src/lib/ventas/pipeline.ts and pipeline.test.ts exist and npm run lint passes on them
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --version</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>vitest installed and configured; pipeline.ts and pipeline.test.ts exist and compile.</done>
</task>

<task type="auto">
  <name>Task 2: PIPELINE_FASE_CONFIG, kanban columns, TRANSITIONS, terminal helpers (PIPE-01)</name>
  <read_first>
    - .planning/phases/ENERSAVE-02-pipeline-domain/02-RESEARCH.md (Badge Colors table, Transition Graph, PipelineFaseConfig interface)
    - .planning/phases/ENERSAVE-02-pipeline-domain/PATTERNS.md (contract-estado const array pattern, kanban column shape from IncidenciasKanban)
    - src/lib/ventas/types.ts (ProspectoFase union — 10 values)
    - src/components/IncidenciasKanban.tsx lines 12-16 (column shape reference only — do not modify this file)
  </read_first>
  <files>src/lib/ventas/pipeline.ts, src/lib/ventas/pipeline.test.ts</files>
  <action>
    Implement PIPE-01 in pipeline.ts per RESEARCH (do not omit backward edges or recontactar restrictions):

    1. export const FASE_INICIAL: ProspectoFase = "prospecto_nuevo"

    2. export interface PipelineFaseConfig with: id, label, kanbanOrder, isTerminal. Omit slaDiasMax, badgeClass, columnAccent in this task — Plan 02-02 adds visual/SLA fields to the same config entries.

    3. export const PIPELINE_FASE_CONFIG: readonly PipelineFaseConfig[] — all 10 fases with Spanish labels and kanbanOrder 1–10 per RESEARCH table order (prospecto_nuevo through descartado; recontactar=9, descartado=10). isTerminal true only for cliente_activo and descartado.

    4. export const PIPELINE_KANBAN_COLUMNS — derive from config sorted by kanbanOrder as readonly { id: ProspectoFase; label: string }[] (accent added in 02-02).

    5. export const TRANSITIONS: Record&lt;ProspectoFase, readonly ProspectoFase[]&gt; — exact adjacency from RESEARCH Recommended TRANSITIONS map:
       - Linear main path with side exits to recontactar and descartado from every non-terminal active fase
       - Backward edges: negociacion→propuesta_enviada, documentacion→negociacion, enviado→documentacion
       - recontactar→contactado and descartado only (no skip-ahead)
       - cliente_activo and descartado: empty arrays

    6. Helpers: getFaseConfig(fase), getProspectoFaseLabel(fase), isTerminalFase(fase), requiresMotivoDescarte(fase) returns fase === "descartado"

    Add pipeline.test.ts describe "PIPELINE_FASE_CONFIG":
    - Assert PIPELINE_FASE_CONFIG length === 10
    - Assert kanbanOrder is unique 1..10
    - Assert isTerminalFase true for cliente_activo and descartado only
    - Assert PIPELINE_KANBAN_COLUMNS ids match config order
  </action>
  <acceptance_criteria>
    - PIPELINE_FASE_CONFIG length === 10 with unique kanbanOrder 1..10
    - isTerminalFase true only for cliente_activo and descartado
    - PIPELINE_KANBAN_COLUMNS ids match config sort order
    - TRANSITIONS includes recontactar/descartado side exits and 3 backward edges per RESEARCH
  </acceptance_criteria>
  <verify>
    <automated>vitest run src/lib/ventas/pipeline.test.ts -t "PIPELINE_FASE_CONFIG"</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>All 10 fases configured with labels, kanban order, terminals flagged; TRANSITIONS graph committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Transition validation API and transition matrix tests (PIPE-02)</name>
  <read_first>
    - .planning/phases/ENERSAVE-02-pipeline-domain/02-RESEARCH.md (validateTransition, TransitionErrorCode, Spanish messages)
    - src/lib/supabase/ventas.ts lines 340-362 (updateProspectoFase consumer — validate before this call in Phase 3)
    - Locked decision: DB trigger owns cambio_fase; pipeline does not write actividades
  </read_first>
  <files>src/lib/ventas/pipeline.ts, src/lib/ventas/pipeline.test.ts</files>
  <behavior>
    - canTransition("prospecto_nuevo", "contactado") → true
    - canTransition("cliente_activo", "contactado") → false
    - canTransition("recontactar", "cualificado") → false (no skip-ahead)
    - getNextFases("descartado") → []
    - validateTransition("contactado", "contactado") → ok:false, code same_fase, Spanish message
    - validateTransition("prospecto_nuevo", "negociacion") → ok:false, code invalid_transition
    - validateTransition("contactado", "descartado") without motivo → ok:false, code motivo_required, message "Indica el motivo de descarte"
    - validateTransition("contactado", "descartado", { motivoDescarte: "sin_respuesta" }) → ok:true
  </behavior>
  <action>
    Export types TransitionErrorCode ("same_fase" | "invalid_transition" | "motivo_required" | "invalid_motivo") and TransitionValidationResult discriminated union.

    Implement:
    - canTransition(from, to): from !== to && TRANSITIONS[from].includes(to)
    - getNextFases(from): TRANSITIONS[from]
    - validateTransition(from, to, options?: { motivoDescarte?: string }):
      Validation order: (1) same_fase — message "El prospecto ya está en esta fase"
      (2) invalid_transition — message includes getProspectoFaseLabel(from) and getProspectoFaseLabel(to): "No se puede pasar de {from} a {to}"
      (3) when to === "descartado" and !options?.motivoDescarte?.trim() — motivo_required
      (4) invalid_motivo check deferred to Plan 02-02 when MOTIVOS_DESCARTE exists — do not implement invalid_motivo yet
      Return { ok: true } on success

    pipeline.test.ts suites:
    - describe "canTransition" / "terminal" — matrix spot-checks including all backward edges and terminal blocks
    - describe "validateTransition" — Spanish messages and motivo_required on descartado

    No Supabase imports. No UI files.
  </action>
  <acceptance_criteria>
    - canTransition rejects same-fase and invalid adjacency per TRANSITIONS graph
    - validateTransition returns Spanish messages for same_fase, invalid_transition, motivo_required
    - validateTransition("contactado", "descartado", { motivoDescarte: "sin_respuesta" }) returns ok:true
    - vitest suites canTransition, validateTransition, and terminal pass
  </acceptance_criteria>
  <verify>
    <automated>vitest run src/lib/ventas/pipeline.test.ts -t "canTransition"</automated>
    <automated>vitest run src/lib/ventas/pipeline.test.ts -t "validateTransition"</automated>
    <automated>vitest run src/lib/ventas/pipeline.test.ts -t "terminal"</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>PIPE-02 satisfied: invalid transitions and descarte-without-motivo rejected with Spanish errors; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client hook → pipeline.ts | Untrusted fase/motivo input from UI must pass validateTransition |
| pipeline.ts → ventas.ts | Only validated transitions should reach updateProspectoFase (Phase 3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | validateTransition | mitigate | Explicit TRANSITIONS graph; unit tests cover matrix including backward edges |
| T-02-02 | Tampering | updateProspectoFase bypass | mitigate | Document consumer contract in pipeline.ts header; Phase 3 hook must call validateTransition first |
| T-02-03 | Repudiation | Invalid fase change | mitigate | validateTransition returns coded errors (same_fase, invalid_transition, motivo_required) for logging/toasts |
| T-02-SC | Tampering | vitest npm install | mitigate | Package Legitimacy Audit [ASSUMED]; pin version in package-lock on install |
</threat_model>

<verification>
- vitest run src/lib/ventas/pipeline.test.ts passes all PIPE-01/PIPE-02 suites
- npm run lint passes
- pipeline.ts has zero Supabase/React imports
- grep confirms TRANSITIONS includes recontactar side exits and three backward edges per RESEARCH
</verification>

<success_criteria>
- PIPE-01: 10 fases with labels, kanban order, terminal detection
- PIPE-02: Transition graph enforced; validateTransition blocks invalid moves and descarte without motivo
</success_criteria>

<artifacts_this_phase_produces>
| Artifact | Plan | Description |
|----------|------|-------------|
| `package.json` (`test` script, vitest devDependency) | 02-01 | Vitest runner |
| `vite.config.ts` (`test` block) | 02-01 | Node test environment |
| `src/lib/ventas/pipeline.ts` | 02-01 | PIPELINE_FASE_CONFIG, TRANSITIONS, canTransition, getNextFases, validateTransition, isTerminalFase |
| `src/lib/ventas/pipeline.test.ts` | 02-01 | Config and transition matrix unit tests |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/ENERSAVE-02-pipeline-domain/02-01-SUMMARY.md` when done
</output>
