---
phase: ENERSAVE-02-pipeline-domain
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260617000005_alter_prospectos_pipeline_v2.sql
  - src/lib/ventas/types.ts
  - src/lib/supabase/ventas.ts
autonomous: false
requirements:
  - DATA-05
user_setup:
  - service: supabase
    why: "Apply DATA-05 migration altering prospectos.fase CHECK and new columns"
    env_vars:
      - name: VITE_SUPABASE_URL
        source: "Supabase Dashboard → Project Settings → API"
      - name: VITE_SUPABASE_ANON_KEY
        source: "Supabase Dashboard → Project Settings → API"
    dashboard_config:
      - task: "Apply migration 20260617000005 via SQL Editor or supabase db push"
        location: "Supabase Dashboard → SQL Editor or local supabase link + db push"
must_haves:
  truths:
    - "prospectos.fase CHECK accepts exactly 11 locked values including tramitacion, pendiente_firma, activado, con_dudas"
    - "Legacy rows remapped: documentacion→negociacion, enviado→tramitacion, cliente_activo→activado"
    - "Six new nullable columns exist on prospectos with subtipo and sub_estado CHECK constraints"
    - "ProspectoFase union in types.ts matches 11-value DB CHECK with no obsolete fases"
    - "mapProspectoRow and updateProspectoFase handle all six new camelCase fields"
  artifacts:
    - path: supabase/migrations/20260617000005_alter_prospectos_pipeline_v2.sql
      provides: "DATA-05 schema migration — 11 fases + 6 columns"
      contains: "prospectos_fase_check"
    - path: src/lib/ventas/types.ts
      provides: "11-value ProspectoFase union + SubtipoProspecto, SubEstadoTramitacion, MotivoDescarte types"
      contains: "tramitacion"
    - path: src/lib/supabase/ventas.ts
      provides: "Row mappers and UpdateProspectoFaseInput for PIPE-05 fields"
      contains: "subtipo_prospecto"
  key_links:
    - from: supabase/migrations/20260617000005_alter_prospectos_pipeline_v2.sql
      to: src/lib/ventas/types.ts
      via: "ProspectoFase union must match CHECK values"
      pattern: "tramitacion|pendiente_firma|activado|con_dudas"
    - from: src/lib/supabase/ventas.ts
      to: src/lib/ventas/types.ts
      via: "mapProspectoRow imports ProspectoFase and new enums"
      pattern: "subtipoProspecto"
---

<objective>
Apply DATA-05: migrate prospectos from 10-fase schema to locked 11-fase pipeline, add six nullable columns, and align TypeScript types and Supabase mappers in the same wave.

Purpose: DB, domain types, and mappers must stay in sync before pipeline.ts or hooks consume new fases and PIPE-05 fields.
Output: Migration file applied on Supabase + updated types.ts and ventas.ts.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/ENERSAVE-02-pipeline-domain/02-RESEARCH.md
@supabase/migrations/20260617000002_create_ventas_core.sql
@src/lib/ventas/types.ts
@src/lib/supabase/ventas.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DATA-05 migration for 11-phase prospectos</name>
  <read_first>
    - .planning/REQUIREMENTS.md (DATA-05 locked table, remap mapping, six columns)
    - .planning/phases/ENERSAVE-02-pipeline-domain/02-RESEARCH.md (DATA-05 Migration Strategy — execution order)
    - supabase/migrations/20260617000002_create_ventas_core.sql lines 14-26 (baseline 10-value CHECK)
  </read_first>
  <files>supabase/migrations/20260617000005_alter_prospectos_pipeline_v2.sql</files>
  <action>
    Create supabase/migrations/20260617000005_alter_prospectos_pipeline_v2.sql following locked execution order per DATA-05 and 02-RESEARCH.md:

    1. DROP CONSTRAINT IF EXISTS prospectos_fase_check on public.prospectos
    2. Remap legacy fase values (locked — not negotiable):
       - documentacion → negociacion
       - enviado → tramitacion
       - cliente_activo → activado
    3. ADD COLUMN IF NOT EXISTS for: subtipo_prospecto, fecha_proximo_contacto, sub_estado, motivo_con_dudas, motivo_recontacto, fecha_recontactar (all nullable timestamptz/text as per REQUIREMENTS)
    4. Backfill sub_estado = 'en_proceso' WHERE fase = 'tramitacion' AND sub_estado IS NULL (migrated enviado rows)
    5. ADD CONSTRAINT prospectos_fase_check with all 11 locked values: prospecto_nuevo, contactado, cualificado, propuesta_enviada, negociacion, tramitacion, pendiente_firma, activado, con_dudas, descartado, recontactar
    6. ADD CONSTRAINT prospectos_subtipo_prospecto_check (nullable OR in base_datos, vecino_zona, contacto_previo, referido)
    7. ADD CONSTRAINT prospectos_sub_estado_check (nullable OR in en_proceso, incidencia_administrativa, pendiente_de_firma)
    8. COMMENT ON COLUMN for subtipo_prospecto and sub_estado documenting PIPE-05 app enforcement

    Do NOT add DB CHECK on transition adjacency (deferred per locked decision). motivo_descarte remains free text at DB level; enum validated in pipeline.ts (PIPE-03).
  </action>
  <acceptance_criteria>
    - Migration file exists with DROP CHECK before UPDATE remap before ADD CHECK
    - grep confirms all three legacy UPDATE remap statements present
    - grep confirms 11 fase values in new prospectos_fase_check
    - grep confirms six ADD COLUMN statements
    - No syntax errors on SQL parse
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^--' supabase/migrations/20260617000005_alter_prospectos_pipeline_v2.sql | grep -c 'documentacion' | awk '{exit ($1>=1)?0:1}'</automated>
    <automated>grep -v '^--' supabase/migrations/20260617000005_alter_prospectos_pipeline_v2.sql | grep -c 'con_dudas' | awk '{exit ($1>=1)?0:1}'</automated>
    <automated>grep -v '^--' supabase/migrations/20260617000005_alter_prospectos_pipeline_v2.sql | grep -c 'subtipo_prospecto' | awk '{exit ($1>=2)?0:1}'</automated>
  </verify>
  <done>DATA-05 migration SQL committed with correct drop-remap-add order and 11-value CHECK.</done>
</task>

<task type="auto">
  <name>Task 2: Update types.ts and ventas.ts for 11 fases + six columns</name>
  <read_first>
    - src/lib/ventas/types.ts (current 10-value ProspectoFase)
    - src/lib/supabase/ventas.ts (ProspectoRow, mapProspectoRow, updateProspectoFase)
    - .planning/phases/ENERSAVE-02-pipeline-domain/02-RESEARCH.md (types.ts Updates, UpdateProspectoFaseInput)
  </read_first>
  <files>src/lib/ventas/types.ts, src/lib/supabase/ventas.ts</files>
  <action>
    Update src/lib/ventas/types.ts per DATA-05:
    - Replace ProspectoFase union: remove documentacion, enviado, cliente_activo; add tramitacion, pendiente_firma, activado, con_dudas (11 total)
    - Add exported types: SubtipoProspecto, SubEstadoTramitacion, MotivoDescarte (9 PIPE-03 ids as union — full MOTIVOS_DESCARTE array lives in pipeline.ts plan 02-03)
    - Extend Prospecto interface with optional: subtipoProspecto, fechaProximoContacto, subEstado, motivoConDudas, motivoRecontacto, fechaRecontactar; narrow motivoDescarte to MotivoDescarte
    - Add UpdateProspectoFaseInput interface with fase + all optional PIPE-05 fields
    - Extend CreateProspectoInput and UpdateProspectoPatch with optional new fields where sensible

    Update src/lib/supabase/ventas.ts:
    - Extend ProspectoRow with six new snake_case nullable columns
    - mapProspectoRow: map all six to camelCase domain fields
    - buildProspectoInsert/buildProspectoUpdate: pass through new fields when provided
    - Refactor updateProspectoFase to accept UpdateProspectoFaseInput (or id + input object) and set row fields: fase, motivo_descarte, subtipo_prospecto, fecha_proximo_contacto, sub_estado, motivo_con_dudas, motivo_recontacto, fecha_recontactar when present in input
    - Keep comment: DB trigger inserts cambio_fase — do not duplicate in client

    Grep repo for obsolete fase string literals (documentacion, enviado, cliente_activo) in src/ and fix any compile breaks in same task scope only — do not refactor unrelated modules.
  </action>
  <acceptance_criteria>
    - ProspectoFase has exactly 11 members; no documentacion/enviado/cliente_activo in types.ts
    - ProspectoRow includes all six new columns
    - mapProspectoRow maps subtipoProspecto from subtipo_prospecto
    - updateProspectoFase accepts UpdateProspectoFaseInput with subEstado for tramitacion
    - npm run lint passes
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
    <automated>grep -v '^--' src/lib/ventas/types.ts | grep -c 'documentacion\|enviado\|cliente_activo' | awk '{exit ($1==0)?0:1}'</automated>
    <automated>grep -v '^//' src/lib/supabase/ventas.ts | grep -c 'subtipo_prospecto' | awk '{exit ($1>=2)?0:1}'</automated>
  </verify>
  <done>Types and mappers aligned with 11-fase DATA-05 schema; lint clean.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3 [BLOCKING]: Apply DATA-05 migration to Supabase</name>
  <read_first>
    - supabase/migrations/20260617000005_alter_prospectos_pipeline_v2.sql
    - .planning/phases/ENERSAVE-01-schema-supabase-ventas/01-01-PLAN.md Task 3 (apply pattern)
  </read_first>
  <files>supabase/migrations/20260617000005_alter_prospectos_pipeline_v2.sql</files>
  <action>
    Apply migration 20260617000005 to linked Supabase project after Tasks 1-2 committed locally.

    Option A (CLI): supabase link --project-ref &lt;ref&gt; then supabase db push

    Option B (Dashboard): Paste migration SQL into Supabase SQL Editor and execute

    Post-apply verification in SQL Editor:
    - SELECT conname, pg_get_constraintdef(oid) FROM pg_constraint WHERE conrelid = 'public.prospectos'::regclass AND conname = 'prospectos_fase_check' — confirm 11 values including con_dudas
    - SELECT column_name FROM information_schema.columns WHERE table_name = 'prospectos' AND column_name IN ('subtipo_prospecto','fecha_proximo_contacto','sub_estado','motivo_con_dudas','motivo_recontacto','fecha_recontactar') — returns 6 rows
    - If test rows existed with legacy fases, confirm none remain: SELECT fase FROM prospectos WHERE fase IN ('documentacion','enviado','cliente_activo') returns 0 rows

    Record apply method and timestamp in plan SUMMARY.
  </action>
  <acceptance_criteria>
    - Migration applies without SQL error
    - prospectos_fase_check enforces 11 values
    - All six new columns visible in Table Editor
    - No rows with obsolete fase values
  </acceptance_criteria>
  <how-to-verify>
    1. Open Supabase Dashboard → Table Editor → prospectos → confirm new columns
    2. Run constraint query above in SQL Editor
    3. Reply "migration applied" or paste error message
  </how-to-verify>
  <verify>
    <automated>MISSING — requires live Supabase; human confirms via checkpoint</automated>
  </verify>
  <resume-signal>Type "migration applied" after verification, or describe errors</resume-signal>
  <done>DATA-05 migration live on Supabase; schema matches types.ts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer → Supabase SQL | Migration ALTER runs with elevated DB privileges |
| Client → prospectos PATCH | New columns writable; PIPE-05 validation deferred to pipeline.ts (Phase 2 plan 02-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Migration remap UPDATEs | mitigate | Locked mapping only; run in transaction; verify 0 legacy rows post-apply |
| T-02-02 | Tampering | CHECK constraint order | mitigate | DROP before UPDATE before ADD per 02-RESEARCH pitfall 1 |
| T-02-03 | Tampering | Free-text motivo_descarte | accept | Enum gate in pipeline.ts (PIPE-03); DB stores text |
| T-02-04 | Information disclosure | New columns nullable | accept | RLS Phase 1 unchanged; columns non-sensitive metadata |
| T-02-SC | Tampering | supabase CLI apply | mitigate | Human checkpoint reviews SQL before Dashboard/CLI execute |
</threat_model>

<verification>
- Migration file present with correct execution order
- npm run lint passes after types/mapper update
- Human checkpoint confirms remote apply
</verification>

<success_criteria>
- DATA-05 satisfied: 11 fase CHECK, six columns, legacy remap, types and mappers aligned
</success_criteria>

<artifacts_this_phase_produces>
| Artifact | Plan | Description |
|----------|------|-------------|
| supabase/migrations/20260617000005_alter_prospectos_pipeline_v2.sql | 02-01 | DATA-05 ALTER prospectos |
| src/lib/ventas/types.ts | 02-01 | 11-fase union + new field types |
| src/lib/supabase/ventas.ts | 02-01 | Mappers + UpdateProspectoFaseInput |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/ENERSAVE-02-pipeline-domain/02-01-SUMMARY.md` when done
</output>
