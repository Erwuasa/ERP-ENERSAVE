---
phase: ENERSAVE-02-pipeline-domain
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - src/lib/ventas/pipeline.ts
  - src/lib/ventas/pipeline.test.ts
autonomous: true
requirements:
  - PIPE-03
  - PIPE-04
  - PIPE-06
must_haves:
  truths:
    - "MOTIVOS_DESCARTE exports exactly 9 locked ids with Spanish labels"
    - "isMotivoDescarte type guard rejects unknown strings"
    - "getSlaUrgencia uses faseChangedAt hours for prospecto_nuevo 4h SLA — not diasEnFase"
    - "getSlaUrgencia uses fechaProximoContacto for contactado custom SLA"
    - "Day-based fases compare diasEnFase against slaDiasMax from config (PIPE-04)"
    - "con_dudas, descartado, recontactar return na urgency"
    - "isSubtipoPrioridadMaxima returns true for all four subtipos (PIPE-06)"
    - "getSlaBadgeClass and getProspectoFaseBadgeClass exported for Phase 4 UI"
  artifacts:
    - path: src/lib/ventas/pipeline.ts
      provides: "SLA urgency, motivos descarte, subtipo priority, badge helpers"
      exports:
        - MOTIVOS_DESCARTE
        - SUBTIPOS_PROSPECTO
        - SUB_ESTADOS_TRAMITACION
        - isMotivoDescarte
        - isSubtipoPrioridadMaxima
        - getSlaUrgencia
        - getSlaBadgeClass
        - SlaUrgencia
        - SlaInput
    - path: src/lib/ventas/pipeline.test.ts
      provides: "SLA hour boundary, motivos, subtipo priority tests"
      contains: "getSlaUrgencia"
  key_links:
    - from: src/lib/ventas/pipeline.ts
      to: src/lib/ventas/types.ts
      via: "Prospecto fields diasEnFase, faseChangedAt, fechaProximoContacto"
      pattern: "SlaInput"
    - from: getSlaUrgencia
      to: PIPELINE_FASE_CONFIG
      via: "getFaseConfig(fase).slaHorasMax / slaDiasMax"
      pattern: "slaHorasMax"
---

<objective>
Complete pipeline.ts with SLA urgency (hours + days + custom date), typed motivos de descarte, subtipo priority (PIPE-06), and badge helpers — all unit-tested.

Purpose: PIPE-03/04/06 deliverables usable by Phase 4 Pipeline UI and Phase 5 Mi Día without reimplementing SLA math or motivo lists.
Output: Extended pipeline.ts + pipeline.test.ts SLA/motivos/subtipo suites green.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/ENERSAVE-02-pipeline-domain/02-RESEARCH.md
@.planning/phases/ENERSAVE-02-pipeline-domain/PATTERNS.md
@src/lib/contract-estado.ts
@supabase/migrations/20260617000003_create_ventas_triggers.sql
@src/lib/ventas/pipeline.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MOTIVOS_DESCARTE, subtipo/sub_estado catalogs, PIPE-06 (PIPE-03, PIPE-06)</name>
  <read_first>
    - .planning/REQUIREMENTS.md (9 motivos descarte, subtipo_prospecto, sub_estado tramitacion)
    - .planning/phases/ENERSAVE-02-pipeline-domain/02-RESEARCH.md (Motivos de Descarte, Sub-estado, PIPE-06)
    - src/lib/incidencias.ts (typed union + const array pattern)
  </read_first>
  <files>src/lib/ventas/pipeline.ts, src/lib/ventas/pipeline.test.ts</files>
  <behavior>
    - MOTIVOS_DESCARTE has exactly 9 entries with locked ids
    - isMotivoDescarte("precio_competencia") true; isMotivoDescarte("precio_no_competitivo") false
    - SUBTIPOS_PROSPECTO has 4 entries matching SubtipoProspecto union
    - SUB_ESTADOS_TRAMITACION has 3 entries
    - isSubtipoPrioridadMaxima returns true for base_datos, vecino_zona, contacto_previo, referido
  </behavior>
  <action>
    Extend pipeline.ts per PIPE-03 and PIPE-06:

    Export MOTIVOS_DESCARTE readonly array of { id: MotivoDescarte; label: string } with exactly 9 locked ids: precio_competencia, no_interesado, permanencia_activa, no_es_decisor, moroso, sin_respuesta, consumo_bajo, ya_es_cliente, otro — Spanish labels from 02-RESEARCH.md. Do NOT use obsolete 10-fase research ids (precio_no_competitivo, etc.).

    Export isMotivoDescarte(value: string): value is MotivoDescarte

    Export SUBTIPOS_PROSPECTO and SUB_ESTADOS_TRAMITACION const arrays with { id, label } for UI selects (Phase 4/6).

    Export isSubtipoPrioridadMaxima(_subtipo: SubtipoProspecto): boolean returning true for all four subtipos per locked PIPE-06 decision.

    Wire isMotivoDescarte into validateTransition descartado branch if not already using it from plan 02-02.

    Add pipeline.test.ts suites: describe("MOTIVOS_DESCARTE") count 9; describe("subtipo") priority max for each id.
  </action>
  <acceptance_criteria>
    - MOTIVOS_DESCARTE.length === 9
    - No obsolete motivo ids in pipeline.ts
    - isSubtipoPrioridadMaxima always true for valid subtipos
    - Tests pass for motivos and subtipo
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/lib/ventas/pipeline.test.ts -t "MOTIVOS|subtipo"</automated>
  </verify>
  <done>Typed motivos, subtipo/sub_estado catalogs, and PIPE-06 priority helper exported and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: getSlaUrgencia — hours, days, custom date (PIPE-03, PIPE-04)</name>
  <read_first>
    - .planning/REQUIREMENTS.md (SLA table — 4h prospecto_nuevo, custom contactado, day SLAs)
    - .planning/phases/ENERSAVE-02-pipeline-domain/02-RESEARCH.md (getSlaUrgencia implementation, Pitfall 2)
    - supabase/migrations/20260617000003_create_ventas_triggers.sql (handle_dias_en_fase floor days)
    - src/lib/ventas/pipeline.ts PIPELINE_FASE_CONFIG sla fields from plan 02-02
  </read_first>
  <files>src/lib/ventas/pipeline.ts, src/lib/ventas/pipeline.test.ts</files>
  <behavior>
    - prospecto_nuevo: hours=3.9 → ok; hours=4 → breach (uses faseChangedAt, diasEnFase=0)
    - cualificado diasEnFase=3 → breach when slaDiasMax=3
    - contactado: past fechaProximoContacto → breach; missing fecha → warning
    - con_dudas → na
    - WARNING_RATIO 0.8: prospecto_nuevo at 3.2h → warning
  </behavior>
  <action>
    Implement SLA layer in pipeline.ts per locked REQUIREMENTS and 02-RESEARCH:

    Export type SlaUrgencia = "ok" | "warning" | "breach" | "na"

    Export interface SlaInput { fase, faseChangedAt, diasEnFase, fechaProximoContacto? }

    Ensure PIPELINE_FASE_CONFIG entries have correct slaHorasMax (4 for prospecto_nuevo), slaDiasMax (3/2/2/5/2/30 for day fases), slaUsesFechaProximoContacto true for contactado, null SLA for con_dudas/descartado/recontactar

    Implement getSlaUrgencia(input: SlaInput, referenceDate?: Date): SlaUrgencia:
    - Hour branch: elapsed hours from faseChangedAt vs slaHorasMax; WARNING_RATIO = 0.8
    - Custom date branch: contactado compares now vs fechaProximoContacto; missing date → warning
    - Day branch: diasEnFase vs slaDiasMax — do NOT recompute days from faseChangedAt in production code (PIPE-04 reads DB field)
    - No SLA config → na

    Critical: never use diasEnFase alone for prospecto_nuevo 4h SLA.

    Add pipeline.test.ts describe("getSlaUrgencia") with fixed referenceDate where needed for deterministic hour boundaries.
  </action>
  <acceptance_criteria>
    - getSlaUrgencia exported with SlaInput accepting Prospecto slice fields
    - Hour SLA test passes at 4h boundary
    - Day SLA uses diasEnFase not client date math
    - Side-lane fases return na
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/lib/ventas/pipeline.test.ts -t "getSlaUrgencia|SLA"</automated>
  </verify>
  <done>getSlaUrgencia handles heterogeneous SLA types correctly with unit tests.</done>
</task>

<task type="auto">
  <name>Task 3: Badge helpers and full test suite gate</name>
  <read_first>
    - src/lib/contract-estado.ts (getContractEstadoBadgeClass pattern)
    - .planning/phases/ENERSAVE-02-pipeline-domain/PATTERNS.md (badge styling text-[9px] font-mono)
    - src/lib/ventas/pipeline.ts (all prior exports)
  </read_first>
  <files>src/lib/ventas/pipeline.ts, src/lib/ventas/pipeline.test.ts</files>
  <action>
    Implement badge presentation helpers mirroring contract-estado.ts:

    getProspectoFaseBadgeClass(fase: ProspectoFase): string — delegate to PIPELINE_FASE_CONFIG badgeClass or switch with dark: pairs for all 11 fases

    getSlaBadgeClass(urgencia: SlaUrgencia): string — ok/warning/breach/na Tailwind classes using text-[9px] font-mono font-bold convention per PROJECT.md

    Ensure getFaseConfig used consistently by getSlaUrgencia and badge helpers.

    Run full pipeline.test.ts suite; fix any failures. Final gate: npm run lint && npm run test.

    Do NOT add UI components — export-only for Phase 4 consumption.
  </action>
  <acceptance_criteria>
    - getProspectoFaseBadgeClass returns non-empty string for each of 11 fases
    - getSlaBadgeClass covers all four SlaUrgencia values
    - Full test file and lint green
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
    <automated>npm run test -- src/lib/ventas/pipeline.test.ts</automated>
  </verify>
  <done>Badge helpers exported; full Phase 2 domain test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → getSlaUrgencia | Client-side urgency display; must not trust diasEnFase for hour SLA |
| Client → motivo_descarte persist | Enum validated in validateTransition before PATCH |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Tampering | Invalid motivo_descarte | mitigate | isMotivoDescarte in validateTransition; MOTIVOS_DESCARTE closed set |
| T-02-10 | Tampering | SLA bypass via stale diasEnFase | mitigate | prospecto_nuevo uses faseChangedAt hour math; day fases read DB dias_en_fase |
| T-02-11 | Information disclosure | SLA urgency labels | accept | Non-sensitive operational metadata |
| T-02-SC | Tampering | npm packages | accept | No new packages in this plan |
</threat_model>

<verification>
- npm run lint && npm run test green
- All PIPE-03/04/06 behaviors covered in pipeline.test.ts
- No obsolete 10-fase or 10-motivo references
</verification>

<success_criteria>
- PIPE-03, PIPE-04, PIPE-06 satisfied: SLA constants + urgency, dias_en_fase day comparison, 9 motivos, subtipo max priority, badge helpers
- Phase 2 domain module complete and ready for Phase 3 hooks
</success_criteria>

<artifacts_this_phase_produces>
| Artifact | Plan | Description |
|----------|------|-------------|
| src/lib/ventas/pipeline.ts | 02-03 | SLA, motivos, badges (extends 02-02) |
| src/lib/ventas/pipeline.test.ts | 02-03 | SLA hour boundary + motivos tests |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/ENERSAVE-02-pipeline-domain/02-03-SUMMARY.md` when done
</output>
