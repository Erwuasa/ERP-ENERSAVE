---
phase: ENERSAVE-02-pipeline-domain
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/lib/ventas/pipeline.ts
  - src/lib/ventas/pipeline.test.ts
autonomous: true
requirements:
  - PIPE-01
  - PIPE-03
  - PIPE-04
must_haves:
  truths:
    - "Each active fase has slaDiasMax constant; terminals and descartado use null (no SLA)"
    - "getSlaUrgencia compares prospecto.diasEnFase input — does not recompute from timestamps"
    - "MOTIVOS_DESCARTE lists 10 typed motivos with Spanish labels for UI selects"
    - "validateTransition rejects unknown motivo ids with invalid_motivo"
    - "Badge and column accent Tailwind classes match ERP conventions with dark: pairs"
    - "getSlaBadgeClass maps ok/warning/breach/na to distinct Tailwind strings"
  artifacts:
    - path: src/lib/ventas/pipeline.ts
      provides: "SLA urgency, motivos descarte, badge/column styling"
      exports:
        - MOTIVOS_DESCARTE
        - MotivoDescarte
        - getSlaUrgencia
        - getSlaBadgeClass
        - getProspectoFaseBadgeClass
        - SlaUrgencia
    - path: src/lib/ventas/pipeline.test.ts
      provides: "SLA and motivo unit tests"
      contains: "getSlaUrgencia"
  key_links:
    - from: src/lib/ventas/pipeline.ts
      to: src/lib/ventas/types.ts
      via: "Prospecto.diasEnFase consumed by getSlaUrgencia(prospecto.diasEnFase, fase)"
      pattern: "getSlaUrgencia"
    - from: src/lib/ventas/pipeline.ts
      to: src/lib/contract-estado.ts
      via: "badge switch pattern"
      pattern: "getProspectoFaseBadgeClass"
    - from: src/lib/supabase/ventas.ts
      to: src/lib/ventas/pipeline.ts
      via: "Phase 3 hook: validateTransition then updateProspectoFase"
      pattern: "validateTransition"
---

<objective>
Complete pipeline domain with SLA thresholds, typed discard reasons, badge/column styling, and diasEnFase-based urgency — ready for Pipeline UI and hooks.

Purpose: PIPE-03 and PIPE-04 — SLA and motivos tipados; UI reads diasEnFase from mapped Prospecto field (DB trigger owns counter).
Output: Extended pipeline.ts exports and full test coverage for SLA/motivos.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/ENERSAVE-02-pipeline-domain/02-RESEARCH.md
@.planning/phases/ENERSAVE-02-pipeline-domain/PATTERNS.md
@.planning/phases/ENERSAVE-02-pipeline-domain/02-VALIDATION.md
@src/lib/contract-estado.ts
@src/lib/ventas/types.ts
@src/lib/supabase/ventas.ts
@supabase/migrations/20260617000003_create_ventas_triggers.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MotivoDescarte union, MOTIVOS_DESCARTE, extend validateTransition (PIPE-03)</name>
  <read_first>
    - .planning/phases/ENERSAVE-02-pipeline-domain/02-RESEARCH.md (Motivos de Descarte section)
    - src/lib/ventas/types.ts (Prospecto.motivoDescarte?: string — keep string on domain type)
    - src/lib/incidencias.ts (IncidenciaTipo union pattern)
  </read_first>
  <files>src/lib/ventas/pipeline.ts, src/lib/ventas/pipeline.test.ts</files>
  <behavior>
    - MOTIVOS_DESCARTE length === 10 with ids matching MotivoDescarte union
    - isMotivoDescarteValid("sin_respuesta") → true
    - isMotivoDescarteValid("invalid_id") → false
    - validateTransition to descartado with motivo "invalid_id" → ok:false, code invalid_motivo
    - validateTransition to descartado with motivo "otro" → ok:true
  </behavior>
  <action>
    Add to pipeline.ts per RESEARCH:

    1. export type MotivoDescarte — union of 10 snake_case ids: sin_respuesta, precio_no_competitivo, permanencia_vigente, no_interesado, fuera_de_perfil, datos_incorrectos, elige_competencia, consumo_insuficiente, suministro_no_viable, otro

    2. export const MOTIVOS_DESCARTE: readonly { id: MotivoDescarte; label: string }[] — Spanish labels from RESEARCH table

    3. export function isMotivoDescarteValid(value: string): value is MotivoDescarte — check against MOTIVOS_DESCARTE ids

    4. Extend validateTransition step (4): when to === "descartado" and motivoDescarte provided but not isMotivoDescarteValid after trim — return { ok: false, code: "invalid_motivo", message: "Motivo de descarte no válido" }

    Do NOT change types.ts Prospecto.motivoDescarte to MotivoDescarte union (RESEARCH open question — keep string; pipeline validates ids).

    pipeline.test.ts describe "MOTIVOS_DESCARTE": all ids present, invalid_motivo path, otro accepted.

    Locked: persist motivo as snake_case text matching id (updateProspectoFase already writes motivo_descarte).
  </action>
  <acceptance_criteria>
    - MOTIVOS_DESCARTE length === 10 with ids matching MotivoDescarte union
    - isMotivoDescarteValid rejects unknown ids
    - validateTransition to descartado with invalid motivo returns code invalid_motivo
    - validateTransition to descartado with motivo "otro" returns ok:true
  </acceptance_criteria>
  <verify>
    <automated>vitest run src/lib/ventas/pipeline.test.ts -t "MOTIVOS_DESCARTE"</automated>
    <automated>vitest run src/lib/ventas/pipeline.test.ts -t "validateTransition"</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>PIPE-03 motivos complete; validateTransition enforces typed motivo ids on descarte.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SLA config, urgency helpers, badge and column classes (PIPE-03, PIPE-04)</name>
  <read_first>
    - .planning/phases/ENERSAVE-02-pipeline-domain/02-RESEARCH.md (SLA Defaults table, urgency thresholds, badge Tailwind table, column accent table)
    - .planning/phases/ENERSAVE-02-pipeline-domain/PATTERNS.md (getContractEstadoBadgeClass switch, contract-penalty guard clauses)
    - src/lib/contract-estado.ts lines 38-58 (badge Tailwind pattern)
    - supabase/migrations/20260617000003_create_ventas_triggers.sql (handle_dias_en_fase — client reads mapped diasEnFase only)
    - Locked: do NOT recompute floor(now - fase_changed_at) in production pipeline code
  </read_first>
  <files>src/lib/ventas/pipeline.ts, src/lib/ventas/pipeline.test.ts</files>
  <behavior>
    - getSlaUrgencia(0, "prospecto_nuevo") with slaDiasMax 2 → "ok"
    - getSlaUrgencia(2, "prospecto_nuevo") with max 2 → "breach"
    - getSlaUrgencia(2, "prospecto_nuevo") at 80% threshold (ceil(2*0.8)=2) → "warning" or "breach" per RESEARCH (>= max is breach; >= ceil(max*0.8) is warning)
    - getSlaUrgencia(5, "cliente_activo") → "na"
    - getSlaBadgeClass("breach") returns rose Tailwind string with dark: variant
    - getProspectoFaseBadgeClass returns distinct classes per fase with border + dark: pairs
    - PIPELINE_KANBAN_COLUMNS includes accent from columnAccent per RESEARCH table
  </behavior>
  <action>
    Extend PipelineFaseConfig and PIPELINE_FASE_CONFIG entries from Plan 02-01:

    1. Add slaDiasMax per RESEARCH SLA table (null for cliente_activo and descartado)

    2. Add columnAccent per RESEARCH kanban table (border-*/30 bg-*/5 pattern)

    3. Add badgeClass per fase — mirror getContractEstadoBadgeClass switch style: bg-*, text-*, border, dark: pairs; slate→sky→cyan→blue→amber→violet→indigo→emerald→purple→rose progression

    4. Update PIPELINE_KANBAN_COLUMNS to { id, label, accent: columnAccent } sorted by kanbanOrder

    5. export type SlaUrgencia = "ok" | "warning" | "breach" | "na"

    6. export function getSlaUrgencia(diasEnFase: number, fase: ProspectoFase): SlaUrgencia
       - Read slaDiasMax from getFaseConfig(fase)
       - null → "na"
       - diasEnFase >= max → "breach"
       - diasEnFase >= Math.ceil(max * 0.8) → "warning"
       - else "ok"
       - PIPE-04: function accepts diasEnFase parameter only — simulates prospecto.diasEnFase from mapProspectoRow; no Date math

    7. export function getSlaBadgeClass(urgencia: SlaUrgencia): string — Tailwind from RESEARCH urgency table (emerald/amber/rose/slate with dark:)

    8. export function getProspectoFaseBadgeClass(fase: ProspectoFase): string — delegate to config badgeClass or switch

    Optional convenience (export if useful for Phase 4): getProspectoSlaUrgencia(prospecto: Pick&lt;Prospecto, "diasEnFase" | "fase"&gt;) wrapping getSlaUrgencia — import Prospecto type from ./types.

    pipeline.test.ts describe "getSlaUrgencia": threshold cases for prospecto_nuevo (max 2), negociacion (max 7), terminal na; use integer diasEnFase inputs only.

    UI convention note in module comment: SLA badges use text-[9px] font-mono font-bold when rendered (Phase 4).

    Do not add UI components or modify ventas.ts in this plan.
  </action>
  <acceptance_criteria>
    - getSlaUrgencia uses diasEnFase parameter only (no Date.now or faseChangedAt)
    - getSlaUrgencia(5, "cliente_activo") returns "na"
    - getProspectoFaseBadgeClass returns Tailwind classes with dark: pairs per fase
    - PIPELINE_KANBAN_COLUMNS includes accent from columnAccent
    - vitest run src/lib/ventas/pipeline.test.ts full suite exits 0
  </acceptance_criteria>
  <verify>
    <automated>vitest run src/lib/ventas/pipeline.test.ts -t "getSlaUrgencia"</automated>
    <automated>vitest run src/lib/ventas/pipeline.test.ts</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>PIPE-03 SLA constants and PIPE-04 diasEnFase-based urgency complete; full pipeline test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → motivoDescarte | Free text from forms must pass isMotivoDescarteValid before persist |
| prospecto.diasEnFase → getSlaUrgencia | DB trigger value trusted; client does not recompute for SLA display |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | motivo_descarte free text | mitigate | MotivoDescarte enum ids; validateTransition invalid_motivo; UI renders labels not raw HTML |
| T-02-05 | Spoofing | Forged SLA urgency | accept | SLA is display-only; diasEnFase from DB via RLS-protected prospecto row |
| T-02-06 | Information disclosure | Motivo labels | accept | Business labels non-sensitive; aggregated in Phase 7 reporting |
| T-02-07 | Denial of service | N/A pure module | accept | Sync functions only; no I/O |
</threat_model>

<verification>
- vitest run src/lib/ventas/pipeline.test.ts — full suite green
- npm run lint passes
- getSlaUrgencia has no Date.now or faseChangedAt references (PIPE-04: reads diasEnFase param only)
- All 10 MOTIVOS_DESCARTE ids match RESEARCH list
</verification>

<success_criteria>
- PIPE-03: SLA per fase and typed motivos with validation
- PIPE-04: getSlaUrgencia consumes diasEnFase from prospecto mapping (parameter), not client-side day recomputation
- Phase 3 can import validateTransition, getSlaUrgencia, getProspectoFaseBadgeClass without UI changes
</success_criteria>

<artifacts_this_phase_produces>
| Artifact | Plan | Description |
|----------|------|-------------|
| `src/lib/ventas/pipeline.ts` | 02-02 | MOTIVOS_DESCARTE, MotivoDescarte, getSlaUrgencia, getSlaBadgeClass, getProspectoFaseBadgeClass, extended PIPELINE_FASE_CONFIG |
| `src/lib/ventas/pipeline.test.ts` | 02-02 | SLA and motivo unit tests |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/ENERSAVE-02-pipeline-domain/02-02-SUMMARY.md` when done
</output>
